#include<bits/stdc++.h>
using namespace std;

int main() {
    int n, w;
    cin>>n;
    vector<int> weights(n), values(n);
    
    for(int i = 0; i<n; i++) {
        cin>>weights[i]>>values[i];
    }

}
